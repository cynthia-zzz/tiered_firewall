# A Tiered, Scalable, and Privacy-Preserving Stateful Firewall

A two-layer eBPF/XDP firewall that combines a **Bloom filter (approximate)** and an **exact flow map** to enforce connections while reducing communication-graph leakage.
