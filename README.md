# A Tiered, Scalable, and Privacy-Preserving Stateful Firewall

A two-layer eBPF/XDP firewall that combines a **Bloom filter (approximate)** and an **exact flow map** to validate packets while reducing information leakage about user communication structure, patterns, and behavior.
